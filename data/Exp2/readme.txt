Here we will be using the translted silver datapoints from one langauge to another to check if additional silver data points are helpful or not.

i)For Bengali, to train the model use the data set: training->(bengali_train_pairsNew.csv+hindiTranstoBengaliNew.csv). test(bengali_test_pairsNew.csv), validation(bengali_val_pairsNew.csv) 

ii)For Hindi, to train the model use the data set: training->(hindi_train_pairsNew.csv+bengaliTranstoHindiNew.csv). test(hindi_test_pairsNew.csv), validation(hindi_val_pairsNew.csv) should be same as it is like the previous experiments.